# HTML 컨벤션 가이드
<br>

## 1. 문서 구조
+ HTML5 표준 문법 사용
+ `<!DOCTYPE html>` 선언 필수
<br>

## 2. 들여쓰기
+ 들여쓰기 : *스페이스 2칸*
<br>

## 3. 시멘틱 태그 사용
+ 의미 있는 코드를 우선적으로 사용
+ 논시멘틱 태그 `div` `span` 남용 금지
<br>

## 4. 접근
+ 로고 IR 기법 사용
+ 이미지에 `alt` 속성 필수
<br>

## 5. 주석 규칙
```
<!-- tag -->
<tag> ... </tag>
```
